@{
    ViewBag.Title = "Index";
}

<h2>Roles select</h2>
@{
    string[] roles = Roles.GetAllRoles();
    foreach (string role in @roles)
    {
        <div class="gallery">@Html.ActionLink(role, "BrowseRole", new {role})</div>
    }
    <br/>
    <p>Total: @roles.Length</p>
    <br/>
}

<h2>User select</h2>

<div>Total users: @ViewBag.TotalUsers</div>
<ul>
    @foreach (MembershipUser user in @ViewBag.Users)
    {
        <li>@Html.ActionLink(user.UserName, "UserDetails", new {user = user.ProviderUserKey})</li>
    }
</ul>

<div>
    Page @(ViewBag.PageCount < ViewBag.PageNumber ? 0 : ViewBag.PageNumber)
    of @ViewBag.PageCount
     
    @if (ViewBag.HasPreviousPage)
    {
        @Html.ActionLink("<<", "Index", new {page = 1, pageSize = ViewBag.PageSize})
        @Html.Raw(" ")
        ;
        @Html.ActionLink("< Prev", "Index", new {page = ViewBag.PageNumber - 1, pageSize = ViewBag.PageSize})
    }
    else
    {
        @:<<
        @Html.Raw(" ")
        ;
        @:< Prev
    }
     
    @if (ViewBag.HasNextPage)
    {
        @Html.ActionLink("Next >", "Index", new {page = ViewBag.PageNumber + 1, pageSize = ViewBag.PageSize})
        @Html.Raw(" ")
        ;
        @Html.ActionLink(">>", "Index", new {page = ViewBag.PageCount, pageSize = ViewBag.PageSize})
    }
    else
    {
        @:Next >
        @Html.Raw(" ")
        @:>>
    }
</div>
<div>
    <p>Users per page:
    @Html.ActionLink("5", "Index", new {pageSize = 5})
    @Html.ActionLink("10", "Index", new {pageSize = 10})
    @Html.ActionLink("20", "Index", new {pageSize = 20})
    @Html.ActionLink("50", "Index", new {pageSize = 50})
    @Html.ActionLink("All", "Index", new {pageSize = 0})</p>
</div>